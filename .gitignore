# Large source PDFs (content-addressed inputs, not code)
docs/Source/*.pdf

# Brainstorm materials (Google Drive export, not code)
AI.Next - Google Folder 17 Jul 2026/

# Node
node_modules/
.next/
out/

# Python
.venv/
__pycache__/
*.pyc
.pytest_cache/

# Env & secrets
.env
.env.*
!.env.example

# Database backups taken on the box by deploy/refresh-content.sh.
# They contain student rows (minors' data in the pilot) — they stay on the box,
# 0600, and never enter git. The checkout at /opt/reletix/AI.NEXT is this repo,
# so this line is what keeps a stray `git add -A` there honest.
/backups/

# OS / editor
.DS_Store
*.log
