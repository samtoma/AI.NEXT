[project]
name = "ainext-extraction"
version = "0.1.0"
description = "AI.Next tutor — content extraction & loading pipeline (spine Pillar I)"
requires-python = ">=3.12"
dependencies = [
    "pydantic>=2.7",
    "psycopg[binary]>=3.2",
]

[tool.uv]
package = false
