# Prototype 1.1 feedback — triage prep

**Snapshot, not a commitment.** This is a planning pass over
`Product Feedback - Prototype 1.1.pdf` against the repo as it stands on
`claude/noor-play-design-system` on 2026-09-19. It exists so the 34 rows on
that sheet can be *filed* efficiently through the `feedback`/`requirement`
skills and *executed* without engineers stepping on each other. **Nothing
here is decided** — triage (Samuel, per [`FEEDBACK.md`](../FEEDBACK.md)) still
owns every outcome. Written to be read once and acted on, not edited in place.

## Before filing anything: the version

The sheet says "Prototype 1.1." The repo has exactly one tag:

```
$ git tag -l 'PDR1-0-*' --sort=-creatordate
PDR1-0-v0.2.0
```

That is almost certainly what this feedback is against — it is literally the
only version that exists to give feedback on. But **confirm with whoever ran
the session** before filing; the `feedback` skill will ask this explicitly
per row and refuses to guess, and so should whoever files these by hand.

## How to use this doc

- **Kind + target skill** tells you whether a row is `/feedback` (an
  observation) or `/requirement` (asking the spec itself to change). Filing a
  requirement-shaped item through `/feedback` will get it filed, but triage
  will just have to reclassify it — do it right the first time.
- **Related FR/SC** is only ever a *pointer* — checked against
  [`specs/001-student-mvp1-delta/traceability.md`](../../specs/001-student-mvp1-delta/traceability.md)
  just now, never invented. Where a row cites one, say so explicitly when
  filing (`/feedback` step 2 asks for exactly this).
- **Quadrant** is engineering-execution guidance (parallel safety), not a
  triage outcome. It answers "who can pick this up and when," not "should we
  build it."

Row 013 does not exist in the source sheet (jumps 012 → 014) — not an
omission here. 033/034 are already `Done` and excluded below. **036 and 037
are new asks from Tamer**, added directly to the table on 2026-09-19 — not
from the printed sheet; see the dated note below for why they were added
here instead of filed as off-sheet GitHub issues.

## Updated 2026-09-19 — session progress

Five commits landed on this branch since the table below was first written
(four committed, one still in the working tree). Status is now tracked per
row in the table.

| Commit | What it did | Rows closed |
|---|---|---|
| `86d5220` | Renamed "Ask the Spine" persona to Noor; dropped the برافو/يلا-بينا Arabic coaching interjections from the math language contract | 022 (as a side effect — that instruction was the actual typo source) |
| `ff17e67` | System prompt addresses the student directly ("to the student X") instead of "about the student X" | 011 |
| `73c30a8` | Found and fixed ~10 spots across `ChatCore`/`ChatQuestionCard`/`CitationChip`/`LessonSession`/`GraphCanvas`/`LoPanel`/`DemoStudentSwitcher`/`api/ask/route.ts` where Arabic rendered unconditionally to English-subject students — root cause was `debug`/mode flags standing in for a language check | 015 (partial — see row) |
| `edee361` | Two more of the same pattern in `ReportCard.tsx` and `StudentLoop.tsx` | 015 (continued) |
| *(uncommitted, working tree)* | `LessonSession.tsx`: voice toggle/mic gated behind `VOICE_UI_ENABLED = false`, backend left wired and dormant | 016 |
| `e02bc7c` *(landed before this session; missed by this table originally)* | Noor Play check-in card rewrite. Verified directly against the pre-rewrite version (`bb0bd1e`) via `git show`/`grep`: `PlayCheckIn` (the Math/English check-in card, `LessonCheckIn.tsx:53-238`) used to render the button "I'm lost" beside an Arabic gloss "مش فاهم حاجة", and called `pct(l.mastery)` to show a raw percentage. Neither appears anywhere in that component's current line range — the button now reads "Walk me through it" (line 157), and there is no `pct(`/`%` call left in it at all. Both `pct()` and "مش فاهم حاجة" still exist further down the same file, but inside `SocialCheckIn` (starts line 287) — a separate, Arabic/Social-only component, out of scope for this row. | 012, 014 |

**Correction (2026-09-19, same day):** row 021 was marked done below and
closed on GitHub as #25 — wrong, per Tamer. What exists (`ResumePrompt` +
`sessionStorage` in `LessonSession.tsx`, text fixed in `73c30a8`) is not what
was asked for, on two counts: (1) `sessionStorage` doesn't persist across
sessions — it survives a reload of the same tab, not a student coming back
later or on another device, which is the actual ask; (2) the intended
behavior isn't "silently reload the exact frame" at all — it's ask a couple
of quick revision questions to reactivate the material, *then* continue.
Reopened on GitHub with both gaps recorded; no implementation attempted here
per instruction.

**Further update, same day — 021 is blocked, not just scoped bigger.** Traced
why durable per-student persistence can't just be bolted onto today's
`students.id`: it's a Postgres `GENERATED ALWAYS AS IDENTITY` sequence, local
to whichever environment's DB it lives in ([`db/schema.sql`](../../db/schema.sql)),
and `load_seed.py`'s normal full-content-reload path
(`TRUNCATE ... students ... RESTART IDENTITY CASCADE`, no `--course` flag)
wipes the table and cascades the delete through `attempts`, `mastery`, and
`sessions`, restarting ids from 1 — a routine content-reseed operation, not a
rare one. On top of that the id is bound to a plain, non-httpOnly,
client-editable cookie that [`lib/demo-student.ts`](../../app/src/lib/demo-student.ts)
itself documents as "NOT an identity claim and NOT a security boundary."
Persisting real lesson history against this id would be silently deleted by
routine reseeds and readable/writable by anyone who edits the cookie.
**021 is now recorded as blocked on the accounts/auth bundle (rows 002–007,
FR-106, currently DEFERRED)** — that work is what introduces a student
identity stable independent of the curriculum-content lifecycle and actually
bound to a person, which durable resume needs as a precondition, not as a
nice-to-have. Sequencing, not scope, was the missing piece in the note above.

## Updated 2026-09-19 (continued) — three more commits, a second session

Picked up row 027 next. Fixing it surfaced two more real bugs along the
way — one in the same file, one entirely off this sheet — both fixed and
filed as their own defects rather than folded silently into 027's fix.

| Commit | What it did | Rows closed |
|---|---|---|
| `7ac26e4` | Numeric-answer grading rejected a correct *expression* ("3x6" for a `correct_answer` of "18") — `grade()` in `api/attempts/route.ts` used `parseFloat`, which silently reads only the leading digits, so "3x6" compared as 3 against 18. Added a small dependency-free arithmetic evaluator (`lib/arithmetic.ts`, whitelisted input, no `eval`/`Function`) that normalises `x`/`×`/`⋅` to `*` and evaluates `+-*/^()` before falling back to the old behaviour. No LLM call added. | not a sheet row — raised directly by Tamer this session, filed and closed as [#40](https://github.com/samtoma/AI.NEXT/issues/40) |
| `18b7d6c` | "Got it — next ✓" never reached grading at all — it's sent as a plain chat message to `/api/ask`, and nothing stopped the model reading it as license to skip a question it never graded (FR-1214). `ChatCore` now derives, straight from the transcript, whether the last pushed question has a matching "the student answered…" event yet; if "Got it" fires while one is still open it's caught client-side, before any network call, with the hardcoded line "Give it a try first or tell me if you need help." | **027** |
| `2578277` | Found while fixing 027: `CheckInCard`'s "no" button was hardcoded Arabic with no English form at all, and `dir="rtl"` was unconditional — on the English Math MVP surface. Same leak class as `73c30a8`/`edee361`, just a component that sweep missed. Both buttons now branch on `arabicUi`. | not a sheet row — filed and closed as [#41](https://github.com/samtoma/AI.NEXT/issues/41) |

All three: `tsc --noEmit` clean, full test suite green (128/128 after the
last), each its own commit, each filed/closed on GitHub with the fix
evidence in the closing comment rather than just asserted here.

## Updated 2026-09-19 (continued) — row 026, a fourth session

026 turned out **not** to be resolved as a side effect of 014, contrary to
this doc's earlier guess (see the old note on 026's row, corrected below).
014 only fixed the check-in *button label* ("I'm lost" → "Walk me through
it"); the actual discouraging claim lived two layers deeper and 014 never
touched either:

| Commit | What it did | Rows closed |
|---|---|---|
| `91c2282` | Two hardcoded, unconditional "the student understood NOTHING" claims, found while investigating a live report of an apologetic tutor opener ("No worries — let's rebuild it from the very first brick"). (1) `lib/lesson.ts`'s `learnPrompt` system prompt told the model this on every learn-mode session, including a lesson never attempted, and also hardcoded the student's grade as a fixed "10" regardless of the actual `students.grade` row. (2) `LessonSession.tsx`'s hidden `autoStart` message — sent to the model as if the STUDENT had typed it, never shown in the transcript — independently claimed "I understood NOTHING… teach me from zero," which was enough on its own to reproduce the apologetic tone even with (1) fixed. Both replaced with `lib/checkin.ts`'s new `learnOpeningFrame`/`learnAutoStartLine`, keyed to the same 0–4 mastery-stage banding the check-in card and mastery ramp already render, so a first-time lesson now opens with upbeat "something new to explore" energy instead of a claimed failure, at every stage. Verified live against the dev DB across all three reachable stages (never-attempted / attempted-not-clicked / mastered) for both the system prompt and the hidden message; `tsc --noEmit` and `eslint` clean. | **026** |

Not filed/closed on GitHub this pass — flagging here first per this
session's instruction; do that next along with the rest of "suggested
filing order" step 0/1 below.

## Updated 2026-09-19 (continued) — row 024, a fifth session

024 turned out to be three separate auto-advance timers in
`LessonSession.tsx`, not one: the tutor's own closing-recap directive
(`{{finish_lesson}}`), a hit turn cap, and review mode reaching its turn
count all fired `requestFinish` on a fixed delay (2.2–2.5s) with no student
action in between — the explanation/recap was on screen but the report
replaced it before there was any chance to read it, exactly as reported.

| Commit | What it did | Rows closed |
|---|---|---|
| `64b45e6` | Removed all three auto-`requestFinish` timers, replacing them with a `readyToFinish` flag that only arms the existing header "Finish lesson" button (now tap-only, no delay) and a matching in-chat suggestion chip — `ChatCore.tsx`'s `suggestions` prop was extended to accept an action chip (`{label, onSelect}`) alongside its existing plain-text form, so the chip fires `requestFinish` directly with no round trip through the model, same as the header button. Also edited `lib/lesson.ts`'s `learnPrompt`/`reviewPrompt` so the tutor's own closing line states the lesson is over, and corrected every `{{finish_lesson}}` doc-comment in the prompt to say it arms the button rather than ending the session (the model is told to keep answering normally if the student keeps chatting past the recap). | **024** |

`tsc --noEmit` and `eslint` clean on all three changed source files. **Not
verified live end-to-end** — this worktree's dev server was already owned by
a concurrent session (Next.js refuses a second `next dev` against the same
`.next` directory even on a different port), and reaching the exact
closing-recap moment needs a full multi-turn LLM lesson conversation.
Confirmed correct by reading the diff instead: all three former
auto-navigate call sites now only set state, and `finish()`/report
navigation is reachable exclusively through a student tap (header button or
chip).

## Updated 2026-09-19 (continued) — priorities set (Tamer)

Added a **Priority** column (P0/P1/P2) to the row-by-row table below, per
Tamer's list:

- **P0:** 001–007, 017, 021, 036
- **P1:** 009, 010, 019, 025, 029, 030, 032, 035, 037
- **P2:** 008, 023, 028, 031

Rows already done or partially done (011, 012, 014, 015, 016, 018, 020, 022,
024, 026, 027, 033, 034) are left unmarked (`—`) — not prioritized since there's
nothing left to schedule. This is a business-priority ranking, distinct from
**Quadrant** (execution-risk/parallel-safety guidance, unchanged above) —
the two can and do disagree, e.g. 021 is P0 but also blocked on 002–007
landing first, and 028/030 are flagged `needs-decision` for Samuel
regardless of priority.

## Updated 2026-09-19 (continued) — related-IDs map

Added a **Related IDs** column to the table below, grouping rows that the
table's own notes already tie together — no relationship here is invented;
each one quotes or paraphrases language already sitting in that row's Notes
cell before this pass. Six clusters, on four different axes (a shared FR, a
shared code surface, a shared epic, or a stated dependency) — some rows sit
in more than one cluster because the axes don't line up:

- **Accounts/auth/role-gating bundle (FR-106):** 002, 003, 004, 005, 006, 007
  — "one role-gating requirement, not four" (005's note), 003 *is* FR-106.
  **021** and **037** aren't members but are both explicitly **blocked on
  this bundle landing** (021 for durable resume, 037 for durable per-user
  identity — same precondition, different consumer).
- **FR-1003 skill-progress display:** 009, 012, 023 — 023 says "file
  together" with 012; 009 touches the same FR with a different ask.
- **Shared UI surface (LessonCheckIn.tsx / student/page.tsx / globals.css):**
  009, 012, 023, 024 — stated directly in the Q4 quadrant note ("still
  shared surface, so 009, 023, 024... should be sequenced"). Overlaps the
  FR-1003 cluster on 009/012/023, but 024 only belongs here, not there.
- **Socratic-method epic:** 020, 029, 030, 031 — "part of the Socratic-method
  epic with 020/030/031."
- **FR-304 content-schema/grounding:** 019, 028 — both cite FR-304 as the
  related requirement, from two different asks (chunking vs. extensibility).
- **Opening/check-in tone copy:** 014, 026 — the doc's own same-day
  correction history: 026 was first guessed fixed by 014, then found to need
  its own separate fix for a different discouraging string.

Rows with no stated relationship to another ID are left `—` in that column,
including two I considered but didn't force in: **017** and **028** both
touch grounding/citation integrity (FR-303/FR-C01 vs. ADR-0007/Principle
III) but nothing in the doc ties them together directly, so that pairing is
a hunch, not a bucket — worth a gut-check with Samuel rather than being
asserted here as fact.

## Updated 2026-09-19 (continued) — two new asks added (036, 037)

Tamer asked for two additional items, not from the printed Prototype 1.1
sheet — added directly to the row-by-row table below (with priorities
attached) rather than filed as off-sheet GitHub issues the way the #40/#41
defects were, since these are triage-shaped asks, not bugs fixed in
passing.

- **036 — P0, "Today" date frozen on return visits.** Traced to a specific,
  verified root cause: the `/spine` Explorer's "Today" toggle
  ([`SpineExplorer.tsx:251`](../../app/src/components/spine/SpineExplorer.tsx))
  labels `data.currentDate` as today, but that value
  ([`lib/queries.ts:186-196`](../../app/src/lib/queries.ts)) is actually the
  bitemporal `system_from` timestamp of the student's last-written
  (still-open) mastery row — it only advances when a new attempt is graded
  and closes/reopens that row
  ([`api/attempts/route.ts:238-246`](../../app/src/app/api/attempts/route.ts)).
  A returning student who hasn't yet attempted anything new sees "Today"
  stamped with a stale date, and every mastery value gated on
  `asOf === "today"` is unchanged alongside it — correct bitemporal
  behavior, mislabeled as a live calendar date. Needs a product decision on
  what "Today" should actually mean before it's a one-line fix (see the
  row). Flagged as the clearest matching instance found in the code — if
  the actual screen this was seen on is different, say so and it gets
  rescoped.
- **037 — P1, per-user identifiable analytics.** More infrastructure
  already exists than the ask assumes — `lib/analytics.ts`'s
  `analytics_events` table already tags every event with `student_id` and a
  server-stamped `environment` (FR-901, VERIFIED), and FR-801 (the PRD §13
  event taxonomy) is already PARTIAL, not absent. The actual gap is durable
  *identity*: the `student_id` on every one of those events is the
  reseed-fragile, client-editable demo cookie (`demo-student.ts`), not a
  real person — the same gap **021** is already blocked on. SC-006 (D7/D30
  retention per environment) is OPEN for the same reason. Filed as riding
  along with the accounts/auth bundle (002–007, FR-106), not a separate
  analytics build.

---

## 2×2 — execution risk (for whoever assigns the work)

### Q1 — Isolated, no design, non-architectural → parallelize freely
008, 011 ✅, 014 ✅, 015 🟡, 016 ✅, 018 ✅, 022 ✅, 026 ✅

Remaining and still genuinely Q1: 008. 015 undersold its own
blast radius — see its row. 026 turned out not to be covered by 014 after
all (see 026's row and the 2026-09-19 fourth-session update above) — it
needed its own fix, now done in `91c2282`. **018 moved here from Q2**: it
looked like a design/legibility question until the root cause turned out to
be a missed `<TeX>` call, not a KaTeX rendering or notation-choice problem —
see its row.

### Q2 — Isolated, needs design first → one owner, contained
009, 010, 032, 036

036's fix is a small, isolated code change once the semantics are decided
— but "what should 'Today' actually mean" is a product call first, same
shape as the rest of this quadrant.

### Q3 — Architectural, shared-surface → sequence, don't parallelize blind
001, 017, 021, ~~027~~ ✅, ~~024~~ ✅, 035

021 turned out bigger than "isolated text fix" once corrected — it now needs
durable server-side persistence plus a new re-engagement interaction, not a
UI tweak. Shared-surface, not Q1/Q2. **Now also blocked on the accounts/auth
bundle (002–007)** — see the addendum above; don't hand this to anyone until
that lands, regardless of sequencing within Q3.

027 is done (`18b7d6c`) — it stayed Q3 rather than dropping to Q1 because
the actual fix touched `ChatCore.tsx`'s shared send/transcript path, not an
isolated string.

024 is done (uncommitted, working tree) — also stayed Q3 rather than Q1: the
fix touched `ChatCore.tsx`'s shared suggestions/send path (same file as 027)
alongside `LessonSession.tsx`'s session-state machine, not an isolated
string or a single-file change.

### Q4 — Architectural + needs design → Samuel + design sign-off before code
002, 003, 004, 005, 006, 007, 012 ✅ (see caveat in its row), 019, 020 🟡 (see caveat in its row), 023, 025, 028, 029, 030, 031, 037

037 sits here rather than with 021 in Q3 because it doesn't share a UI
surface with the auth bundle — it shares a *precondition* (durable
per-person identity). Same "don't schedule before 002–007 lands" rule
applies regardless of quadrant.

012 shipped without the Samuel/Tamer sign-off this quadrant is supposed to
require — the code changed (as part of `e02bc7c`), the requirement
conversation didn't happen. Worth noting as a process gap, not just a closed
row.

**Formerly a standing conflict, now resolved:** [`app/src/components/student/LessonCheckIn.tsx`](../../app/src/components/student/LessonCheckIn.tsx),
[`app/src/app/student/page.tsx`](../../app/src/app/student/page.tsx), and
`globals.css` were uncommitted mid-rewrite when this doc was first drafted
(the Noor Play sticker design system) — that rewrite is now committed
(`e02bc7c`) and `git status` is clean on all three. Still shared surface,
so 009, 023, 024 (012 is done) landing here should still be sequenced, not
parallelized blind — just not blocked on in-flight work anymore.
Land or stash that work before handing any of those four to a second agent.

---

## Full row-by-row filing table

**Grouped by relationship, not strict ID order** — each related-IDs cluster
(see the note above) sits as a contiguous block, placed at the position of
its lowest-numbered member; 021 is pulled up next to the auth bundle it's
blocked on. Use the ID column to look up a specific row rather than assuming
ascending order.

| ID | Status | Observation (condensed) | Kind → skill | Related FR/SC | Quadrant | Priority | Related IDs | Notes |
|---|---|---|---|---|---|---|---|---|
| 001 | ⚪ Not started | Deploy PDR1-0 on a public link | *not feedback* | — | Q3 | P0 | — | This is Phase 3 of the in-flight refactor in [`docs/WIP-branch-per-solution.md`](../WIP-branch-per-solution.md), not a fresh ask. Track it there; don't file through `/feedback`. |
| 002 | ⚪ Not started | Admin accounts + roles | defect → `/requirement` | none found (gap) | Q4 | P0 | Auth bundle → 003–007 | No FR covers roles today; [`lib/demo-student.ts`](../../app/src/lib/demo-student.ts) is explicitly "not auth." New requirement, not a bug. |
| 003 | ⚪ Not started | End users can sign up | defect → `/requirement` | **FR-106** (DEFERRED) | Q4 | P0 | Auth bundle → 002, 004–007 | This *is* FR-106. Filing it as feedback would just rediscover a decision already recorded in `decisions.md` Q5 — file as a requirement change proposing to un-defer FR-106, and it inherits SC-004's "stale, needs Tamer" problem. **021 depends on this landing** — see 021's row and the 2026-09-19 addendum above; un-deferring FR-106 unblocks durable lesson-resume, not just signup. |
| 004 | ⚪ Not started | Landing page for signup | defect → `/requirement` | — | Q4 | P0 | Auth bundle → 002, 003, 005–007 | Same bucket as 003; blocked on it. |
| 005 | ⚪ Not started | Hide "pipeline" tab from end users | defect → `/requirement` | none found (gap) | Q4 | P0 | Auth bundle → 002–004, 006, 007 | Bundle with 002/006/007 — one role-gating requirement, not four. |
| 006 | ⚪ Not started | Hide "content" tab from end users | defect → `/requirement` | none found (gap) | Q4 | P0 | Auth bundle → 002–005, 007 | Same bundle as 005. |
| 007 | ⚪ Not started | Hide "evidence walk" tab from end users | defect → `/requirement` | none found (gap) | Q4 | P0 | Auth bundle → 002–006 | Same bundle as 005. |
| 021 | 🔴 Reopened — **blocked on 002–007** | Resume a lesson instead of restarting | defect → `/feedback` | none found (gap) | Q3 | P0 | Blocked on auth bundle → 002–007 | Corrected 2026-09-19 (Tamer): the existing `ResumePrompt`/`sessionStorage` mechanism in `LessonSession.tsx` (text fixed in `73c30a8`) doesn't persist across sessions — `sessionStorage` clears when the tab/session ends. The actual ask is also a different interaction than "reload the exact frame": ask a couple of quick revision questions first, then continue, not silently resume. Needs durable (server-side) persistence + a new re-engagement flow — scope as a `/requirement`, not a quick fix. **Blocked**: `students.id` is a reseed-fragile, per-environment, unauthenticated identity (see addendum above) — not sound to hang durable history off until the accounts/auth bundle (002–007, FR-106) lands. Don't schedule before that. |
| 037 | ⚪ Not started | Add analytics/event tracking identifiable per user | product → `/feedback` | **FR-801** (PARTIAL), **FR-901** (VERIFIED), **SC-006** (OPEN) | Q4 | P1 | Same precondition as 021; blocked on auth bundle → 002–007 | New ask (Tamer, 2026-09-19), not on the printed sheet. More already exists than the ask assumes: [`lib/analytics.ts`](../../app/src/lib/analytics.ts)'s `analytics_events` table already tags every event with `student_id`, `session_id`, and a server-stamped `environment` (FR-901, VERIFIED — never client-set); `ai_interactions` separately logs cost/token events per `student_id` too. FR-801 (emit the PRD §13 taxonomy) is PARTIAL — traceability.md's only listed gaps are `parent_view_opened` and the Phase-11 safety events, not identity. **The real gap is durable identity, not tracking plumbing:** `student_id` here is [`demo-student.ts`](../../app/src/lib/demo-student.ts)'s reseed-fragile, client-editable cookie id (see 021's row/addendum) — not bound to a real person, so a routine content reseed (`TRUNCATE ... RESTART IDENTITY CASCADE`) silently reassigns it and analytics can't say "this is the same person" across visits, reseeds, or devices. SC-006 (D7/D30 retention per environment) is OPEN for exactly this reason. Same precondition as 021: lands with the accounts/auth bundle (002–007, FR-106), not as a separate analytics build. |
| 008 | ⚪ Not started | Remove per-button/section explanatory microcopy | product → `/feedback` | — | Q1 | P2 | — | Pure deletion, no design gate needed. |
| 009 | ⚪ Not started | Friendly UI for skill progress (owner: Tamer) | product → `/feedback` | **FR-1003** (VERIFIED) | Q2 | P1 | FR-1003 → 012, 023; shared surface → 012, 023, 024 | FR-1003 already mandates bands + %; this asks for a friendlier *presentation* of what's required, not a change to the requirement — unlike 012/023 below. |
| 012 | ✅ Done (`e02bc7c`) | Remove percentages from the study page | **defect → `/requirement`** | **FR-1003** (VERIFIED — mandates showing % + band) | Q4 | — | FR-1003 → 009, 023; shared surface → 009, 023, 024 | Verified directly: `PlayCheckIn` (`LessonCheckIn.tsx:53-238`, the Math check-in card) has no `pct(`/`%` anywhere in it — only band colors. **But it shipped without the `/requirement` conversation** — FR-1003 still says percentage MUST be shown, and nobody has decided whether to amend the spec or revert the code. File the `/requirement` anyway, now framed as "reconcile FR-1003 with what's already live," not "should we do this." |
| 023 | ⚪ Not started | "92%, that's excellent" mid-lesson is confusing | **defect → `/requirement`** | **FR-1003** (VERIFIED) | Q4 | P2 | FR-1003 → 009, 012; shared surface → 009, 012, 024 | Same FR-1003 tension as 012 — file together, will likely resolve as one requirement change. |
| 024 | ✅ Done (`64b45e6`) | Last question skipped the explanation, jumped to report | defect → `/feedback` | **FR-1218** (the refutation must reach the student) | Q3 | — | Shared surface → 009, 012, 023 | Confirmed: three timers in `LessonSession.tsx` (`onFinishDirective`, `onCapped`, and review mode's turn-cap check) auto-fired `requestFinish` 2.2–2.5s after the closing recap/explanation rendered, cutting it off before the student could read it. Replaced with a `readyToFinish` flag — the header "Finish lesson" button and a new matching in-chat suggestion chip (`ChatCore.tsx`, action-chip support added) are now the only way to reach the report; `lib/lesson.ts`'s closing-message instructions were also updated so the tutor's own last line says the lesson is over. `tsc`/`eslint` clean; not verified live (see the 2026-09-19 fifth-session note above) — this worktree's dev server is owned by a concurrent session. |
| 010 | ⚪ Not started | Chat with the spine should be its own page | product → `/feedback` | — | Q2 | P1 | — | Currently a dock ([`AskSpineDock.tsx`](../../app/src/components/chat/AskSpineDock.tsx)), not a route. |
| 011 | ✅ Done (`ff17e67`) | Tutor should speak first-person, not third | product → `/feedback` | — | Q1 | — | — | Verified live on `/spine`: replies "I'm Ask the Spine…" and addresses the viewer directly. Not independently re-verified on the `/student` lesson prompt path — same fix, different call site, worth a quick check before closing. |
| 014 | ✅ Done (`e02bc7c`) | Rewrite "I'm lost" copy | product → `/feedback` | — | Q1 | — | Opening-tone copy → 026 | Verified via `git show bb0bd1e` against the current file: the button literally read "I'm lost" beside an Arabic gloss "مش فاهم حاجة" and now reads "Walk me through it" (`LessonCheckIn.tsx:157`). Note: `MODE_COPY.learn.autoStart` in `LessonSession.tsx` still says "I understood NOTHING from today's lesson" — but that string is the hidden message *sent to the model* (`autoStartHidden`), never shown to the student, so it doesn't undo this fix; it's a different surface from the one this row is about. |
| 026 | ✅ Done (`91c2282`) | New-lesson copy feels discouraging ("you don't understand anything") | **defect → `/feedback`** | **FR-1009** (VERIFIED — forbids "you're behind" framing) | Q1 | — | Opening-tone copy → 014 | Was NOT resolved by 014 (that only changed the check-in button label) — `MODE_COPY.learn.autoStart` in `LessonSession.tsx` still said "I understood NOTHING… teach me from zero," sent to the model as the student's own hidden first message, and `lib/lesson.ts`'s `learnPrompt` system prompt independently made the same claim plus hardcoded the student's grade as "10". Both replaced with mastery-stage-based copy (`lib/checkin.ts` `learnOpeningFrame`/`learnAutoStartLine`) that never claims failure, verified live across all three reachable stages against the dev DB. Cite FR-1009 when filing/closing on GitHub. |
| 015 | 🟡 Partial (`73c30a8`, `edee361`) | Force teaching language to English; add an eval | product → `/feedback` | — | ~~Q1~~ **Q3 in hindsight** | — | — | The app's own hardcoded UI strings no longer render Arabic to English-subject students — that half is done, across 11 files, and it was **not** isolated the way it was first triaged (touched shared chat/lesson/report components). Still missing: the LLM-judge eval harness the action item actually asked for, and this fix does nothing to constrain what the *model itself* generates in freeform text. |
| 016 | ✅ Done, uncommitted | Remove Voice for now | product → `/feedback` | — | Q1 | — | — | Landed as `VOICE_UI_ENABLED = false` in `LessonSession.tsx` — backend (`lib/voice.ts`, `lib/tts-client.ts`, `api/tts/route.ts`) stays wired and dormant per the code comment, not deleted. Still sitting uncommitted in the working tree. |
| 017 | ⚪ Not started | Explanation drifted to a previous question | **defect → `/feedback`** | **FR-303 / FR-C01** (VERIFIED/BUILT — grounding assembled before every model call) | Q3 | P0 | — | Cite FR-303 explicitly when filing; this is a violation, not a gap. |
| 018 | ✅ Done (`dd286b0`) | Math notation (`$a:b=c:d$`) unreadable | **defect → `/feedback`** | — | Q1 | — | — | Not a legibility/design question after all — the "Why That Happened" refutation panel ([`ChatQuestionCard.tsx:289`](../../app/src/components/chat/ChatQuestionCard.tsx)) rendered `st.text_md` as raw text instead of through `<TeX>`, so any `$...$` segment (e.g. `$a:b=c:d$`) showed as the literal string instead of rendered math. Every other call site carrying the same field shape — the question stem and choices two lines above in this same file, plus canonical-solution steps in `QuestionModal.tsx`/`ReviewStage.tsx` — already routes through `<TeX text={...} />`; this was the one spot that didn't. Fixed by wrapping it the same way. `tsc --noEmit` clean; the file's one `eslint` error (`Date.now` in `useRef`, line 38) is pre-existing and unrelated — confirmed by stashing this fix and re-running lint. Not verified live (needs a seeded question with an authored refutation containing `$...$` plus a real wrong-answer attempt through the chat flow) — confirmed correct by pattern-matching against three working call sites instead. |
| 019 | ⚪ Not started | Chunk explanations into steps | product → `/feedback` | **FR-304/FR-305** (content-model context) | Q4 | P1 | FR-304 content-schema → 028 | Needs a content-schema change (library entries are monolithic today) plus the human-review pipeline — file as feedback first, expect it to become a requirement if accepted. |
| 028 | ⚪ Not started | Explanations limited to book content, not extensible | **requirement change, not feedback** | **FR-C01 / FR-304 / constitution Principle III** | Q4 | P2 | FR-304 content-schema → 019 | Sheet itself says "let's discuss together" — this presses directly on the grounded-teaching principle ADR-0007 already carved one exception into. Needs an ADR-level conversation with Samuel, not a ticket. |
| 020 | 🟡 Partial (`7ac26e4`) | Tutor should understand step-work, not just final answers | product → `/feedback` | **FR-1211/1216/1217** (widget modality already supports non-final-answer signals) | Q4 | — | Socratic epic → 029, 030, 031 | Only the narrowest slice is covered: a numeric-answer text box now grades a single arithmetic *expression* that evaluates to the right value ("3x4" for 12), not just the bare final number (#40). Still missing, and this is most of the actual ask: multi-step or algebraic work (the fix only evaluates arithmetic — `+-*/^()` — not algebra), anything spanning more than one expression, and any partial-credit or diagnose-where-it-went-wrong judgment on a multi-step solution. FR-1211/1216/1217 (widgets reporting structural predicates) are the closer match for that, but T119 (PROJECT_STATE.md) — no tutor turn has ever actually *chosen* a widget — means even that path is unproven live. File as feedback, scoped to what's still missing; likely belongs with the Socratic-method epic (029/030/031) rather than as its own fix. |
| 029 | ⚪ Not started | Tutor locks in an idea without confirming the student solved it | product → `/feedback` | none found (gap) | Q4 | P1 | Socratic epic → 020, 030, 031 | Part of the Socratic-method epic with 020/030/031. |
| 030 | ⚪ Not started | No Socratic method; no open-ended questions; doesn't end on retrieval | product → `/feedback` | **SC-005** (OPEN — "the core bet") | Q4 | P1 | Socratic epic → 020, 029, 031 | Frame as direct evidence toward SC-005 when filing; this is the biggest single item on the sheet. |
| 031 | ⚪ Not started | No request for intermediate steps, only final answers | product → `/feedback` | — | Q4 | P2 | Socratic epic → 020, 029, 030 | Same epic as 030. |
| 022 | ✅ Done (`86d5220`) | "Bravo" typo variants (brafo, brofa...) | defect → `/feedback` | — | Q1 | — | — | Root cause was an instruction in `subjects.ts` telling the model to sprinkle transliterated برافو/يلا-بينا — removed. File as closed, with the root cause noted, not just "seems fixed." |
| 025 | ⚪ Not started | Add "review my weak spot" as a third post-lesson option | product → `/feedback` | — | Q4 | P1 | — | New session-selection logic + mastery ranking; `lesson_review` surface already exists in [`api/ask/route.ts`](../../app/src/app/api/ask/route.ts) turn caps, so the backend seam is partly there. |
| 027 | ✅ Done (`18b7d6c`) | "Got it" without attempting marks the question correct | **defect → `/feedback`** | **FR-1214** (VERIFIED — client reports structure, server decides meaning) | Q3 | — | — | Not actually a grading bypass — the acknowledgement never reached `grade()`; it was the model reading an unstructured "Got it" as license to move on. Fixed deterministically in `ChatCore.tsx`, no LLM call. Closed on GitHub as [#31](https://github.com/samtoma/AI.NEXT/issues/31). |
| 032 | ⚪ Not started | Too many textbook/standard references confuse the student | product → `/feedback` | — | Q2 | P1 | — | Provenance display calibration ([`ProvenanceBadge.tsx`](../../app/src/components/ProvenanceBadge.tsx)). |
| 033 | ✅ Done | Rename to Noor | — | — | Done | — | — | Extended in `86d5220` to a persona string in `ask.ts` the original pass missed. |
| 034 | ✅ Done | Children's design system | — | — | Done | — | — | Matches this branch's in-flight work. |
| 035 | ⚪ Not started | Cost tracking by function | needs-decision → `/feedback` | **FR-C04** (BUILT — `ai_interactions` already carries `surface_kind`) | Q3 | P1 | — | Sheet already says "Blocked — discuss with Sam." Worth checking whether this is a query/dashboard task against existing `surface_kind` tagging rather than a new schema — smaller than it first looks. |
| 036 | ⚪ Not started | "Today" date freezes at first use; rest of interface doesn't update on return | defect → `/feedback` | none found (gap) | Q2 | P0 | — | New ask (Tamer, 2026-09-19), not on the printed sheet. Root cause verified in the `/spine` Explorer's Baseline/Today toggle: `currentDate` ([`lib/queries.ts:186-196`](../../app/src/lib/queries.ts)) is set to `new Date(row.system_from).toISOString()` — the write-timestamp of the still-open mastery row (`system_to IS NULL`) — not the actual wall-clock date, and rendered as `` `Today · ${fmtDate(data.currentDate)}` `` in [`SpineExplorer.tsx:251`](../../app/src/components/spine/SpineExplorer.tsx). That row only gets a new `system_from` when [`api/attempts/route.ts:238-246`](../../app/src/app/api/attempts/route.ts) closes and reopens it on a graded attempt — so a student who returns without yet attempting anything new sees "Today" stamped with the date of their *last* mastery change (which can be day one), and every value gated on `asOf === "today"` (mastery %, graph coloring) is frozen alongside it. Correct bitemporal behavior, mislabeled as calendar-today — needs a product decision on what "Today" should actually show (real date vs. last-updated date, possibly both) before it's just a one-line fix. If what you're seeing lives on a different screen than `/spine`, flag it — this is the clearest matching instance found, not a guess elsewhere. |

---

## Suggested filing order

0. **Close out what's already done first** (011, 012, 014, 016, 022, 026, and
   015 partially, 033/034 already were) — 011, 016, 022 filed as closed/fixed
   on GitHub (#16, #21, #26); #20 (015) left open with a partial-status
   comment. **012, 014 and 026 aren't filed as closed on GitHub yet** —
   012/014 found/verified by code inspection, 026 fixed directly in
   `91c2282` — none filed there this pass; do that next.
   **021 (#25) is not on this list** — closed once, then reopened the same
   day; see its row. 016 is now committed (`b3b51a5`), so it's actually done,
   not just done-in-the-working-tree. **027 is now also done** (`18b7d6c`,
   closed as [#31](https://github.com/samtoma/AI.NEXT/issues/31)) — remove it
   from step 1 below. **024 is now also done** (uncommitted, working tree;
   see the fifth-session note above) — not filed/closed on GitHub yet.
1. ~~**Cite-an-existing-FR defects** (017, 026, 027)~~ — 026 and 027 done;
   017 remains, cheapest to file, cheapest to argue for, no design debate.
2. **The two FR-1003 items together** (012, 023) — of a different shape than
   originally scoped: 012 already shipped (see its row), so the conversation
   is now "amend FR-1003 or revert the code," not "should we do this." 023
   (the mid-lesson "92%, that's excellent" phrase) is a separate surface —
   LLM-generated chat text, not a static UI element — and nothing here
   verified whether the model still says it. Don't assume 023 is covered.
3. **The signup/roles bundle** (002–007) as one `/requirement` proposal against FR-106, not six. **037** rides along with this bundle once it lands — file it alongside, not separately.
4. **028 and 030** flagged for Samuel directly — these are the ones docs/FEEDBACK.md calls `needs-decision`, not ordinary triage.
5. **036** needs a product decision on what "Today" should mean (see its row) before it's filed as a plain defect — flag it with 028/030 rather than filing blind.
6. Everything else through `/feedback`, in any order — none of it is blocking.

**Scoreboard:** 11 of 36 rows done or partially done (011, 012, 014, 015
partial, 016, 018, 020 partial, 022, 024, 026, 027), plus 033/034 which were
already done when this doc was first written. 021 was briefly counted done,
corrected same-day to not done — see its row. 24 rows untouched, including
the two new asks (036, 037) added 2026-09-19.

**Off-sheet, same session:** two more real defects found and fixed while
working this list, neither from the printed sheet — a numeric-answer
grading bug Tamer hit directly ([#40](https://github.com/samtoma/AI.NEXT/issues/40))
and the `CheckInCard` Arabic leak found while fixing 027
([#41](https://github.com/samtoma/AI.NEXT/issues/41)). Both filed and
closed with fix evidence, same as every row above.
